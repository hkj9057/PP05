#include "MGameLoop.hpp"

int main()
{
	MuSeoun_Engine::MGameLoop gLoop;
	gLoop.Run();

			//git
	        //init = 새로운 git 저장소를 생성할때 사용 
	        //commit = 소스코드에서 변경된 내용을 로컬 저장소에 저장
	        //push = 로컬 저장소의 작업을 원격 저장소에 업로드
			//pull = 다른 사람이 원격 저장소에 업로드한 것을 로컬저장로소 가져옴/ 변경된 사항 가져옴
			//clone = 원격저장소에서 그대로 로컬 저자오에 다운로드함
			//staging 커밋할 파일을 미리 선택해 스테이징 에리어에 올림/
			//작업물 ->add -> staging area -> commit -> 로컬저장소
			//branch 각자의 영역에서 소스코드 변경 후 병합 가능

			//namespace 함수의 이름이나 변수의 이름이 같아지면 이름충돌이 발생하므로 namespace로 소속을 정해줌
			//using 지시자 = using namespace~ = ::없이 네임스페이스안 사용가능
			//씽크 어씽크 = 동기(scv)/ 비동기(프로브) 
	        

	return 0;
}